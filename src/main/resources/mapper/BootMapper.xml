<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xf.study.admin.mapper.BootMapper">
    <!--public Boot getBoot(Long id);-->
    <select id="getBoot" resultType="xf.study.admin.bean.Boot">
        select * from boot where id=#{id}
    </select>

</mapper>